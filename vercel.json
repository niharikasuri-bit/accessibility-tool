{
  "version": 2,
  "buildCommand": "pnpm --filter @digit-a11y/ui run build",
  "outputDirectory": "packages/ui/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
