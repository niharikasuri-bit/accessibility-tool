/**
 * Global test setup placeholder.
 *
 * Per-environment setup happens inside individual test files (or
 * package-local setup files) because vitest resolves dynamic imports
 * here at transform time, which breaks the "only-in-DOM" trick.
 */