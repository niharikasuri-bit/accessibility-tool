{
  "name": "@digit-a11y/explorer",
  "version": "0.1.0",
  "private": true,
  "description": "Sitemap-anchored dynamic explorer — visits a fixed URL list, exercises each page's dynamic states, and scans them with axe. Navigation comes only from the sitemap; click-navigation is never followed.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "test": "vitest run",
    "studio": "node run-studio.mjs",
    "lint": "echo \"(explorer lint pending)\""
  },
  "dependencies": {
    "playwright": "^1.44.0",
    "@digit-a11y/scanner": "workspace:*",
    "@digit-a11y/reporter": "workspace:*"
  }
}
