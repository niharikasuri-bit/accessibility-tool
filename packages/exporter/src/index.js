/**
 * @digit-a11y/exporter — PDF and JSON export of FriendlyReport objects.
 *
 * Implemented in Phase 1 Day 7:
 *   - PDF via Playwright `page.pdf()` rendering a templated HTML report
 *   - JSON export endpoint (matches API response shape exactly)
 */
export async function exportPdf(/* report */) {
  throw new Error('exportPdf not yet implemented — Phase 1 Day 7');
}

export function exportJson(/* report */) {
  throw new Error('exportJson not yet implemented — Phase 1 Day 7');
}
